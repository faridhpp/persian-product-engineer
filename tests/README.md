# Validation Tests

This directory contains checks to ensure the Skill package keeps a valid structure.

## Purpose

- Verify required files exist
- Prevent accidental breaking changes
- Keep the repository compatible with AI agent workflows
